name: deploy
on:
  push:
    branches:
      - master
jobs:
  build-and-deploy:
    runs-on: ubuntu-latest
    steps:
    - name: Checkout Master
      uses: actions/checkout@master

    - name: Build and Deploy
      env:
        BUILD_SCRIPT: yarn && yarn run deploy
